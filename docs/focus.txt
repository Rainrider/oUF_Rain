JS Hunter Bar for oUF

for SetPoint

	mainShotFocusCost = select(4, GetSpellInfo(spellID))
	powerBarMaxValue = select(2, frame:GetMinMaxValues())
	powerBarWidth = Power:GetWidth()
	
	mainShotFocusCost * powerBarWidth / powerBarMaxValue

Problem is availability of powerBarWidth and powerBarMaxValue - check on PostUpdatePower?

FocusGainPerdiction still to do